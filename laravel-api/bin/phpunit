#!/bin/bash
# Runs laravel php unit tests with xdebug enabled
# Usage: bin/phpunit
# Usage: bin/phpunit --filter CategoryRepositoryTest
. bin/helpers/common.sh

docker-compose exec --user=laradock workspace \
    php -d xdebug.remote_host=172.18.0.1 \
    ./vendor/bin/phpunit ./tests/ $@

